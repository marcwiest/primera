(function () {
    'use strict';

    var $ = jQuery || {};
    var navbar = (function (options) {
      var opt = $.extend({
        animation: "slide",
        animationSpeed: 250
      }, options);
    });

    console.log(navbar);

}());
//# sourceMappingURL=front-page.js.map
